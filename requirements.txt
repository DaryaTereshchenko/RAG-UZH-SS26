# CPU-only requirements for the RAG Lecture assignments.
#
# Suggested setup (conda):
#   conda create -n rag-lecture python=3.10 -y
#   conda activate rag-lecture
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements.txt
#
# The PyTorch CPU wheel must be installed first from the PyTorch index
# (the line above) so that pip does not try to pull the default CUDA build
# from PyPI. The remaining packages can then be installed normally.

# Core scientific stack
numpy>=1.24,<2.0
scipy>=1.10

# NLP / tokenization
nltk>=3.8

# IR dataset loader used for the Cranfield corpus
ir_datasets>=0.5.5

# Hugging Face stack (CPU inference + tokenization)
transformers>=4.40,<5.0
tokenizers>=0.15
huggingface_hub>=0.20
safetensors>=0.4
sentencepiece>=0.1.99
accelerate>=0.27

# Notebook runtime
jupyter>=1.0
ipykernel>=6.25
ipywidgets>=8.0

# Progress bars used by HF / ir_datasets
tqdm>=4.65
